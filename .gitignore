# Local Claude Code config (contains absolute Windows paths)
.claude/

# Editor + OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Dependencies
node_modules/

# Large local-only video files (raw episode recordings)
*.mp4
*.mov
*.avi

# the eleven short clips behind /the-first-ten/ are deliberate (about 0.9 MB in total)
!assets/wall/*.mp4
